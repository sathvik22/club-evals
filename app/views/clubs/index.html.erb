<h1>Student Organizations</h1>
<%= form_tag(clubs_path, :method => "get", id: "search-form") do %>
  <%= text_field_tag :search, params[:search], placeholder: "Search Student Organizations" %>
  <%= submit_tag "Search", :name => nil %>
<% end %>
	<div class="row">
		<div class="columns">
			<ul class="accordion" data-accordion data-allow-all-closed="true">
				<% @clubs.each do |club| %>
					<li class="accordion-item" data-accordion-item>
				 		<a href="#" class="accordion-title"><%= club.name %></a>
   					<div class="accordion-content" data-tab-content >
      				<p><%= club.description %></p>
							<div>
								<% club.evals.each do |eval| %>
								<p>Rating: <%= eval.rating %> <br/>Hours per Week: <%= eval.hours  %> <br/> Review: <%= eval.review %></p> 
								<%end%>
							</div>
      				<%= link_to 'Evaluate this club!', new_club_eval_path(club.id) %>
    				</div>
					</li>
			  <% end %>
	  	</ul>
		</div>
	</div>
